<div class="container-fluid p-4">
  <h1 class='fs-5'>My Portfolio</h1>
  <% if current_user&.buyer? %>
    <%= render 'balance' %>
  <% end %>
  <%= "Your portfolio is empty" if current_user.stocks.empty? %>
  <% if !@stocks.empty? %>
    <%= render 'transactions/stocks_list' %>
  <% end %>
</div>
