<div class="container">
  <p>My Portfolio</p>
  <%= "Your portfolio is empty" if current_user.stocks.empty? %>
  <% if !@stocks.empty? %>
    <%= render 'transactions/stocks_list' %>
  <% end %>
</div>
